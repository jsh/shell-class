But what about when I call it without args?

multi-pick  # not a useful error message.

We can make the error message more useful.
Don't say what's wrong, say what's right.

Oh, but what if we give it a bogus arg?

multi-pick xxx

Whoops.
