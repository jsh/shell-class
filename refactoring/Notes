Okay, let's refactor our lottery-pick script
to see what we can learn.

(1) Let's split it into two programs.
Whoops! That doesn't work. Why?

(2) Add a path. Okay, that works.

(3) Add a path a nicer way. What's that saying?

(4) Okay, let's execute it from somewhere else.

cd
~-/pick

Whoops. That doesn't work either. Why?

(5)
