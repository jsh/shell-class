$ somecommand
It works...
$ echo $?
0
$ badcommand
it fails...
echo $?
1
$

->
  $ true # always succeeds
  $ echo $?
  0
  $ false # always fails
  echo $?
  1
  $

  (These give the reader something real to try, and are useful commands to know
  about.)

$ somecommand
...
$ if (( $? )); then echo failed; else echo OK; fi

->

  if somecommand; then echo failed; else echo OK; fi

  (Idiomatic and common.  Anyway, you haven't introduced (()) yet.)


