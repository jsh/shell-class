printf "%b" "\n" -> echo

  (Even if you use printf() for other messages, "echo" for a simple newline is
  idiomatic.  Here, as with 3.6, the extra "%b" is visual noise.)

Some older scripts may use s to disable the screen echo ->
  Some older scripts may use stty to disable the screen echo

  (Typo.)
