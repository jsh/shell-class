if [ "$directory" = "Finished" ]; then ->
  if [ "$directory" == "Finished" ]; then

  (You use "==" for the loop test.  They're identical, but until you talk about
  this binary operator, consistency will minimize reader confusion.)

