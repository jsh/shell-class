when you get to ${10} you better use the braces or else teh shell will interpret that as ${1} followed immediately by the literal string 0

  (It's interesting, at least to me, that this is the opposite of $X0, which
  the shell takes to mean ${X0}, not ${X}0.)
