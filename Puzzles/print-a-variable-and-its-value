Write a function that prints a variable and its value.

Example:

$ source printval.sh
$ x=3
$ printval x
x is '3'

(a) Write it using "${!"
(b) Write it using "declare -n"

If you don't know "${!" or "declare -n", look them up the bash man page.
